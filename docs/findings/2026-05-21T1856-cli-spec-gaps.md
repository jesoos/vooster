---
title: CLI Spec Gaps — queued for a follow-up CLI goal
created_at: 2026-05-21T18:56:42Z
resolved: partial
resolved_by:
  - 4ed0f3b
  - 7c8b6ec
status_notes: |
  2026-06-03 re-verified: only open item is public conflict setup;
  blocked on product decision for the mechanism (reuse change propose/commit
  flow vs dedicated public endpoint). Current proof still relies on
  /__test/.../revisions setup endpoints. KEEP partial.
  Missing-verbs section emptied by Goal 9 (closed).
  2026-05-26: CLOSED lock release --format=agent (4ed0f3b).
  2026-05-26: CLOSED vspec --help grouping and vspec help <command> routing (7c8b6ec).
  OPEN: merge resolve public conflict setup remains blocked on test infra/product surface: public CLI can create/open/resolve merges, but cannot create divergent branch/main revisions; the current proof still relies on /__test/.../revisions setup endpoints.
related:
  - docs/07-cli-spec.md
  - docs/findings/2026-05-22T1632-dogfood-snapshot.md
  - docs/findings/2026-05-23T1700-dogfood-followups.md
  - goals/7-cli-spec-parity.md
  - goals/9-cli-trim.md
---

# CLI Spec Gaps — queued for a follow-up CLI goal

This file enumerates surfaces from `docs/07-cli-spec.md` that are
documented in the spec but **not implemented** in the current CLI
dispatcher (`apps/cli/src/index.ts`). Goal 7 deliberately scoped these
out (see `goals/7-cli-spec-parity.md` "Scope Guards" — _"No new CLI
verbs beyond `init`"_); each item here is the queue for the next CLI
goal.

Each entry: surface — current state — first observed.

## Missing verbs (write/read commands the spec promises)

Goal 9 moved this queue out of findings:

The over-engineered self-teaching verbs were dropped from the spec, and
the read-path verbs were promoted into `goals/9-cli-trim.md` as the active
implementation scope. Keep this section free of bullet entries until a new
spec-promised CLI verb is discovered outside Goal 9's declared scope.

## `--format=agent` coverage debt

Goal 7 standardized the agent envelope (`buildAgentEnvelope`), and Goals 9
and 10 promoted the first project/actor/stakeholder/goal/usecase surfaces.
The spec (`docs/07-cli-spec.md:16`) implies `--format=agent` is a global
output mode applicable to every verb. All agent-format coverage debt from this
finding is closed. Closed item:

- CLOSED: `lock release` — implemented as `vspec lock release <lock-id>
--format=agent`.

Each candidate needs: a `--format` flag in its CLI flags, an agent
branch routed through `buildAgentEnvelope`, plus the `data`/`context`
mapping from the API response. The gate `7.A3` will then enumerate
each new file automatically (source of truth: `grep -rl 'format ===
"agent"' apps/cli/src/commands/`).

Note: `step edit` now supports `--format=agent`, but its API response lacks
`revision.id`; the agent envelope's context.revision is therefore null. A later
API contract goal can expose the revision id.

## Honest E2E setup gaps

- `merge resolve public conflict setup`

`merge resolve --format=agent` is implemented, but honest public CLI setup for
resolvable conflicts remains queued because current conflict setup depends on
`__test` endpoints instead of public CLI-only setup.

## Help system surface

Closed 2026-05-26 by 7c8b6ec:

- `vspec --help` now prints grouped command families instead of the global
  oclif flag dump.
- `vspec help <command>` and `<command> --help` route to the same
  command-specific renderer.

## Resolution evidence

- 4ed0f3b added `DELETE /v1/locks/:id`, `vspec lock release <lock-id>`, and
  agent-envelope unit + honest CLI coverage.
- 7c8b6ec added grouped root help, routed `help <command>`, and e2e help
  coverage.
- `bash scripts/completion-check.sh` passed after both changes.

## Notes for the next goal's iteration

When picking up this queue, prefer the order:

1. Read verbs that hosts of agents will hit first
   (`actor show`/`list`, `usecase set`, `goal show`).
2. Agent-envelope rollout to write-path verbs (so agents see uniform
   shape across `create` outputs).
3. `doctor` as a server-backed renderer.
4. Per-verb `--help` routing (the same pattern Goal 7 used for
   `init`).

Do **not** silently widen `HONEST_UC_SET` in `goals/7-*.gates.sh`;
this debt is for the next goal to take and re-scope honestly.
