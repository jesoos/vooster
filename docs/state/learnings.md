# Learnings

_Append-only. One bullet per learning. Keep it terse — the cumulative file is
read every iteration._

- **colima `--disk` 가 재시작만으로는 안 늘어남** (v0.10.1 기준). `colima delete -f` 도 `~/.colima/_lima/_disks/<profile>/datadisk` 파일을 지우지 않아서 기존 사이즈가 재사용됨. 사이즈 변경 순서: `colima stop` → `rm -rf ~/.colima/_lima/_disks/<profile>` → `colima delete -f` → `colima start --disk <N>`. 호스트 디스크는 멀쩡한데 postgres 가 `pg_wal` 에 "No space left on device" 를 띄우면 의심 1순위는 VM 데이터디스크 풀.
- **gate fixture race**: ESLint boundary fixtures written under `apps/*/src` can race `_meta` typecheck; if a gate must write such fixtures, make them typecheck-safe or move the check to an API that does not touch the tree.
- **ignored mirror docs need explicit handling**: when a required mirror file is gitignored, formatting tools may skip it and `git add` will reject it; format the tracked source, mechanically mirror it, then `git add -f` the ignored mirror.
- **overnight findings sweep closure**: F1/F2 activation goals, CLI help/lock-release gaps, gates-over-coupling goals 7-29, and harness docs are closed; remaining partials are scoped deferrals (merge-resolve public setup, A14 planned verbs, route-test Phase 2, HONEST_UC_SET derivation).
- **2026-05-27 findings+meta-audit sweep**: closed/advanced 6 findings (harness-spec-debt#3, CI shard, Gap B, invocation-links, shared-contracts package-shape domains, route-test Phase 2 partial). Follow-up audit closed the cheap agent-contract 4b cut (`actor restore`), clarified shared-contracts accounting as 21 planned package slices plus the separately tracked Doctor slice, and migrated `doctor-routes` to app.inject integration. Route-test Phase 2 is 1/37.
- **2026-06-02 cycle 260602-01 Tier 0 honesty pass**: re-verified in-scope findings vs code. shared-api-contracts (stakeholder-interest + impact routes still route-local — these are the deferred "non-package-shape surfaces"), agent-contract-followups (format_version split + member/workspace unroutable suggestions still OPEN), route-test Phase 2 (36 unit files; lock/sync have Phase-1 integration exemplars but unit files NOT removed → still unmigrated, only doctor fully migrated). No stale CLOSED-claimed-but-open items found; notes were accurate. Refined route-test note to flag the lock/sync fold-into-exemplar nuance.
- **2026-06-02 cycle 260602-01 meta-audit #1** (after work-units Tier0 + T1-1): (Q2/check-test-codes) trimmed `apps/cli/tests/unit/init-command.test.ts` — the `expect(fetch).toHaveBeenCalledWith(url, {exact RequestInit})` asserted the exact header-construction form; behavior is already covered by the written-config assertion, so kept the endpoint assertion with `expect.anything()` for RequestInit (same overfit class the contracts migration already removed for actor/stakeholder commands). (Q3) Large gates (7/8/9/4/6 at 419–592 LOC) were already reviewed/trimmed under the gates-over-coupling finding (resolved: true) — no new evidence to re-trim, leaving them KEEP. (Q5 defer) `session-list-routes.ts` retains a tiny local `heartbeatSchema` ({last_activity_at}) and inline sessionId param parse — a non-package-shape leftover the shared-contracts finding already defers; not worth a goal. P9 rebuttal: "should we add a gate forbidding route-local request z.object?" — rejected: it would fossilize form (Q4) and spuriously fail on legit local param parses; contracts adoption is better driven per-domain by the finding than by a blanket grep gate.
- **2026-06-02 cycle 260602-01 meta-audit #2** (after T1-2 impact + T1-3 format_version): (Q7) the format_version consolidation required a prior-goal gate change — handled as goal-design §5 case (b): gate 7.A2 logic + goal-7.md A2 prose re-aligned in one commit, invariant moved from read=1/mutation=2 to a single v1 (stronger), tests tightened to ===1 first. (Q1) gate 7.A2's version-literal grep is now redundant with ~15 runtime `format_version===1` assertions, but it co-locates with a must-keep negative-universal ("no format_version outside the two envelope modules") — KEEP, since re-trimming the just-touched gate adds risk with zero product value. (Q5) only 2 CLI read sites still cast `.body as <type>` (impact revision-list, status.ts) — recorded in the shared-contracts finding as the deferred typed-client tail rather than ad-hoc fixed.
- **2026-06-02 cycle 260602-01 T1-4 DEFERRED (measured, not skipped)**: option (b) "reason-only suggestions" looked like a small S change but `suggestedNextActionSchema` (command required) is reused by 17 contract response schemas and CLI printers read `action.command` unconditionally — so making command optional is a contract-wide loosening + per-printer null-safety sweep + ~10 API sites + ~10 tests. Too cross-cutting to land safely unattended (finding marks it "after beta"); deferred to a dedicated reviewed slice. Lesson: measure the shared-type reuse graph before classifying a "make field optional" change as small.
- **2026-06-02 cycle 260602-01 meta-audit #3** (after sync + 13-file route-test batch): audited the migration for honesty — all new integration tests assert real status + problem `title` through `startServer()`/app.inject real routing (spot-checked merge, revision-history, usecase-test, api-key; grep confirms none are assertion-free). This is the check-test-codes philosophy applied: mocked "handler returned this shape" units replaced with real-Fastify-wiring verification. P9 rebuttal "is this just re-testing zod/typecheck?" fails — integration covers route registration + body-parser + schema hookup + problem-response shape that static tools don't. Deferred the finding's "Maybe Phase 2" gate idea ("every \*-routes.ts has an integration importer") until the back-catalog migration completes — adding it now would fail for the 22 still-unmigrated files (Q4 don't fossilize a half-done state).
- **2026-06-02 cycle 260602-01 meta-audit #4 + route-test finding CLOSED**: Phase 2 migration complete (37-file queue: 33 fully migrated to app.inject integration via uc-fixtures, 4 trimmed to genuinely-HTTP-unreproducible defensive branches as documented accepted exceptions). Flipped route-test-coverage-honesty to resolved: true. (Q2/§1.5) Reconsidered the finding's own "every \*-routes.ts has an integration importer" gate idea and REJECTED promoting it: app.inject integration tests use server.fetch (no module import) and paths are dynamic template strings, so any grep gate would be form-coupled — exactly the §1.5 anti-pattern. Enforcement is the established convention (33 exemplars) + existing coverage gate + review, not a brittle grep. Honest-residual principle: a mocked unit trimmed to the single defensive branch unreachable via real HTTP is an accepted exception, not the anti-pattern. Parallel-subagent migration (2 agents, disjoint file sets, no commits) worked well; independently verified chain-green + spot-checked lock/session honesty before committing.
- **2026-06-02 cycle 260602-01 COMPLETE**: closed route-test-coverage-honesty (resolved: true; Phase 2 33/37 migrated + 4 documented residuals); advanced shared-api-contracts (stakeholder-interest + impact domains -> @vooster/contracts) and agent-contract-followups (format_version consolidated to single v1, goal-7 gate §5 case (b)); deferred T1-4 reason-only suggestions (contract-wide blast radius) and recorded 2 remaining .body-as casts as the typed-client tail. 4 meta-audits. Parallel subagents migrated the route-test side-effect tail efficiently (independently verified chain-green + spot-checked honesty before each commit). Final: API 776 + CLI/contracts 276 tests green, completion-check exit 0.
- **2026-06-03 cycle 260603-01 meta-audit #1** (after Tier0 honesty + Tier1 3-cast): trimmed `impact-agent-format` unit fetch method/body assertions. Contracts + honest impact e2e own API request-shape drift; the unit now stays output-focused on human/agent rendering.
- **2026-06-03 cycle 260603-01 meta-audit #2** (after 1827 L1/L2): trimmed Goal 36 `[36.B2]` implementation-token grep (`SCHEMA_INVALID`/`allowed_values`/`apiErrorCodeSchema`). Q1/Q2: `usecase-route`, `usecase-results`, and `error-codes` tests already enforce the coded field-guidance behavior and CLI code parsing; keeping the grep would fossilize helper names. Kept `[36.B1]` as a negative-universal literal ban, and kept Goal 35 `spec_language` grep as the finding's structural acceptance anchor.
- **2026-06-03 cycle 260603-01 meta-audit #3** (after L4+trust-gap T1): trimmed Goal 37 `stepImplementationRefSchema`/`steps.unlinked` greps because contracts + doctor tests already enforce those behaviors; kept the Prisma `Step.implements` grep as the persistence-schema anchor. Also narrowed the new `step-agent-format` unit assertion from exact fetch body to the behavior under test (`implements` forwarded), leaving request-shape drift to contracts/e2e.
- **2026-06-03 cycle 260603-01 meta-audit #4** (after trust-gap T2/T3): trimmed Goal 39's action/workflow grep block because `verify-action.test.ts` already enforces the composite Action command path, exit-code output, step summary, local workflow `uses: ./`, and PR-comment log outputs. Kept the `--verify-workflow` docs/help/source grep as the only structural init-advertisement anchor.
- **2026-06-03 cycle 260603-01 COMPLETE**: completed Tier 0 honesty notes, Tier 1 CLI read-path contract parsing, 1827 L1/L2/L4, and trust-gap T1-T4 (Step.implements -> deterministic `vspec verify` -> GitHub Action adapter -> honest deterministic drift copy). Four meta-audits trimmed overfit unit/gate checks. Final in-scope verification: `bash scripts/completion-check.sh` exit 0; remaining dogfood-loop worktree edits are separate from this cycle.
